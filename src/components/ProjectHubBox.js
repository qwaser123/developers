import styled from "styled-components";

export const ProjectHubBox = styled.div`
width: ${(props) => props.width};
background-color: white;
height: ${(props) => props.height};
border-radius: 10px;
padding: 10px;
padding-bottom: 0;
margin-left: ${(props) => props.marginLeft};
`;